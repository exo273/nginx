services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nginx-sigr.rule=Host(`alpha.kvernicola.cl`)"
      - "traefik.http.routers.nginx-sigr.entrypoints=websecure"
      - "traefik.http.routers.nginx-sigr.tls.certresolver=letsencrypt"
      - "traefik.http.services.nginx-sigr.loadbalancer.server.port=80"
      - "traefik.docker.network=dokploy-network"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - restaurant-network
      - dokploy-network

networks:
  restaurant-network:
    external: true
  dokploy-network:
    external: true
